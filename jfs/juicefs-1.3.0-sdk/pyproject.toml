# jfs/juicefs-1.3.0-sdk/pyproject.toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "juicefs"
version = "1.3.0"

[tool.setuptools.package-data]
"juicefs" = ["libjfs.so"]
